[package]
name = "agentrix-desktop"
version = "0.1.1"
edition = "2021"
description = "Agentrix Desktop 鈥?AI Agent floating ball"

[lib]
name = "agentrix_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[[bin]]
name = "agentrix-desktop"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon", "devtools", "custom-protocol"] }
tauri-plugin-global-shortcut = "2"
tauri-plugin-notification = "2"
tauri-plugin-shell = "2"
tauri-plugin-autostart = "2"
tauri-plugin-updater = "2"
tauri-plugin-store = "2"
tauri-plugin-http = "2"
tauri-plugin-dialog = "2"
tauri-plugin-process = "2"
tauri-plugin-clipboard-manager = "2"
reqwest = { version = "0.12", features = ["stream", "json"] }
tokio = { version = "1", features = ["fs", "io-util"] }
mdns-sd = "0.11"
hostname = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
image = { version = "0.25", default-features = false, features = ["png"] }
zip = "2"

# Codex-borrow P1 — Computer Use cross-platform input + capture.
# `enigo` handles Windows + macOS keyboard/mouse synthesis; `xcap` covers
# screen + window enumeration on both. We deliberately avoid embedding a
# Chromium runtime — see docs for system-Chrome CDP rationale.
enigo = { version = "0.3", features = ["serde"] }
xcap = "0.0.14"
base64 = "0.22"
tokio-tungstenite = { version = "0.24", features = ["rustls-tls-webpki-roots"] }
futures-util = "0.3"

[target.'cfg(windows)'.dependencies]
webview2-com = "0.38"
windows = "0.61"

[dependencies.rfd]
version = "0.15"

[profile.release]
opt-level = "s"
codegen-units = 16
lto = false
strip = true
